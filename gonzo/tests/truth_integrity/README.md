# `truth_integrity` — 18 case, freeze trước Gate 1

Xem `gonzo/tests/README.md` §① cho luật đầy đủ và ngưỡng.

Nguồn duy nhất để viết các case này là luật trong `gonzo-vault/governance/` — đặc biệt
`grounding-policy.md` và `roles.md`. **Không** đọc code của hệ mới khi viết.

Khi đủ 18 case: commit, tag, và không sửa nữa trừ khi luật vault đổi (và luật vault đổi thì
phải đi qua `vault-approver` + một decision record, không sửa lén trong lúc build).

Trạng thái: **trống — 0/18 case.**
