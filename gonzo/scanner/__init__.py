"""Pre-load truth gate for self-written memory and skills (D-15).

Procedural learning stays free; factual learning must go back to the vault. The
scan runs before a file is loaded into context, not as an audit after it breaks.
"""
